{# 学情报告 Markdown 模板 #}
# {{ student_name }} · {{ exam_label }} · {{ subject }}学情分析与提分建议

> **考试**：{{ exam_full_name }} · {{ duration }} 分钟 · 闭卷
> **总分**：**{{ total_scored }} / {{ total_full }}**（失 {{ total_lost }} 分）
> **学生**：{{ student_name }}　**准考证号**：{{ student_id }}
> **整体定位**：{{ overall.overallVerdict }}

---

## 一、整体得分概览

| 题型 | 题号 | 学生得分 | 满分 | 失分 |
|------|------|---------|------|------|
{% for row in score_table -%}
| {{ row.section }} | {{ row.qRange }} | **{{ row.scored }}** | {{ row.full }} | {{ row.lostLabel }} |
{% endfor -%}
| **合计** | — | **{{ total_scored }}** | {{ total_full }} | **−{{ total_lost }}** |

**判读**：

- 共失分 **{{ wrong_count }} 道题**，扣 {{ total_lost }} 分。
{% for cause in overall.lostMainCauses -%}
- 主因 {{ loop.index }}：**{{ cause.cause }}**（涉及 {{ cause.involvedQuestions | join("、") }}，共 {{ cause.lostPoints }} 分）
{% endfor %}

---

## 二、失分逐题分析

{% for q in wrong_questions %}
### 第 {{ q.id_num }} 题（{{ q.type_label }} · 失 {{ q.lost }} 分）

#### 原题

{% if q.stem -%}
> {{ q.stem }}
{%- if q.options %}
>
{%- for opt in q.options %}
> - {{ opt.label }}. {{ opt.text }}
{%- endfor %}
{%- endif %}
{% else -%}
> （题干详见原卷）
{%- endif %}

**标准答案**：**{{ q.correct_label }}**

**学生答案**：**{{ q.student_label }}**{% if q.lost == q.full %} ❌{% else %} ⚠️{% endif %}

#### 错误点分析

**知识点**：{{ q.analysis.knowledgePoint }}　|　**错误类型**：{{ q.analysis.errorType }}

{{ q.analysis.rootCause }}

{% if q.analysis.comparisonTable -%}
| 选项 | 学生判断 | 正解判断 | 解释 |
|------|---------|---------|------|
{% for row in q.analysis.comparisonTable -%}
| {{ row.item }} | {{ row.studentJudgment }} | {{ row.correctJudgment }} | {{ row.reason or "" }} |
{% endfor %}
{%- endif %}

**核心考点**：{{ q.analysis.keyInsight }}

#### 改进建议

{{ q.analysis.improvement }}

---
{% endfor %}

## 三、提分建议（按优先级排序）

{% for p in overall.improvementPriorities %}
### {{ ['🔴','🟡','🟢','🔵'][loop.index0] }} 优先级 {{ p.priority }}：{{ p.topic }}（预期可补 {{ p.expectedGain }}）

{{ p.rationale }}

**具体动作**：
{% for a in p.actions -%}
- {{ a }}
{% endfor %}

{% endfor %}

---

## 四、{{ "二模" if "一模" in exam_label else "下次考试" }}前备战清单

| 周次 | 重点 | 量化目标 |
|------|------|---------|
{% for w in overall.prepPlan4Weeks -%}
| 第 {{ w.week }} 周 | {{ w.focus }} | {{ w.quantifiedTarget }} |
{% endfor -%}

**预期目标**：{{ overall.secondMockTarget }}

---

## 五、本次考试的肯定面

{% for p in overall.positives -%}
- {{ p }}
{% endfor %}

---

> 报告生成：{{ generated_at }} · pipeline.py v1
> 数据来源：{{ exam_label }} · {{ subject }}（试卷结构化 + 学生答题卡 OCR + 小分表）
